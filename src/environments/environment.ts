// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firestone: {
    apiKey: 'AIzaSyDxmE5HcYB_B1EKL7XG75IACjz90rZfJ-U',
  authDomain: 'zooft-visit.firebaseapp.com',
  databaseURL: 'https://zooft-visit.firebaseio.com',
  projectId: 'zooft-visit',
  storageBucket: 'zooft-visit.appspot.com',
  messagingSenderId: '49322514408',
  appId: '1:49322514408:web:aca85d8e3c1608da55765d'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
